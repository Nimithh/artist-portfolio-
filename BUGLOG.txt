BUG LOG -- Artist Portfolio Platform
Append-only. Number bugs sequentially (BUG #001, #002, ...). Do not delete
old entries, even after they're fixed.

===========================================
BUG #001
DATE FOUND: 7/2/2026
FOUND BY: Claude (during the code audit Nimith requested)
STATUS: FIXED
-------------------------------------------
WHAT WAS WRONG:
- The 20-artwork cap trigger (enforce_artwork_limit) could be beaten by
  two inserts running at the same time. Both would count 19 existing
  artworks, both would pass the check, and the artist would end up with
  21 artworks.

CAUSE (root cause, if known):
- Classic check-then-act race: the trigger counted rows and then allowed
  the insert, but nothing stopped a second transaction from doing the
  same count in parallel before either insert was committed. The count
  can't see the other transaction's uncommitted row.

HOW IT WAS FIXED:
- The trigger now locks the artist's row in tbl_artists (select ... for
  update) before counting. Two inserts for the same artist are forced to
  run the check one at a time, so the second one sees the first one's
  row and gets rejected. The function was made security definer so the
  lock works regardless of the caller's RLS policies.
- Verified with a live two-session test on local Postgres 16: artist at
  19 artworks, two concurrent inserts, second one correctly blocked then
  rejected, final count exactly 20.
- The new report flood cap trigger (enforce_report_limit) uses the same
  lock-then-count pattern so it does not inherit the same bug.

FILES CHANGED:
- supabase/migrations/20260701000002_create_tbl_artworks.sql
- supabase/migrations/20260701000003_create_tbl_reports.sql (same
  pattern applied preventively)

FIXED BY: Claude (approved by Nimith) on 7/2/2026
===========================================

===========================================
BUG #002
DATE FOUND: 7/2/2026
FOUND BY: Nimith (hit the error in the dashboard), diagnosed by Claude
STATUS: FIXED
-------------------------------------------
WHAT WAS WRONG:
- Artwork upload (and profile photo upload) always failed with the generic
  "Could not save the artwork" message. Server log showed the real error:
  "Could not find the 'image_height' column of 'tbl_artworks' in the
  schema cache."

CAUSE (root cause, if known):
- The migrations originally run against the live Supabase project were an
  older version of the schema from a duplicate project folder (see the
  7/2 PROGRESS entry about the parallel-sessions mixup). The code expects
  the newer schema: tbl_artworks was missing image_public_id, image_width,
  image_height, medium, year_created, updated_at and the race-safe cap
  trigger; tbl_artists was missing photo_public_id; tbl_reports was
  missing status and the flood-cap trigger.

HOW IT WAS FIXED:
- One combined reset script: dropped the 3 tables and helper functions,
  then re-ran this repo's 5 migration files in order. Nimith executed it
  in the Supabase SQL editor. Verified afterward column-by-column over the
  REST API that the live schema matches the code. Only test data was lost.

FILES CHANGED:
- None in the repo (live database only). The throwaway reset-database.sql
  was deleted after use.

FIXED BY: Nimith (ran the script) + Claude (diagnosis and script) on 7/2/2026
===========================================
