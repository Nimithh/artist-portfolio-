PROGRESS LOG -- Artist Portfolio Platform
Append-only. Do not delete or overwrite old entries.

===========================================
DATE: 7/1/2026
WORKED ON BY: Nimith
-------------------------------------------
DONE TODAY:
- Read and adopted CLAUDE.md (renamed from CLAUDE_NIMITH.md so it auto-loads
  each session) and DEV-PROMPT-PACK.md as the project's governing rules.
- Proposed and got approval for the database schema: tbl_artists,
  tbl_artworks, tbl_reports.
- Scaffolded the Next.js project at the repo root (TypeScript, Tailwind CSS,
  App Router, ESLint).
- Wrote Supabase migration SQL in supabase/migrations/:
  - tbl_artists (1:1 with auth.users, slug/bio/skills constraints, updated_at
    trigger)
  - tbl_artworks (title/description constraints, BEFORE INSERT trigger that
    enforces the 20-artwork cap at the database level)
  - tbl_reports (reason restricted to stolen_art/inappropriate/other)
  - RLS policies: public read on artists/artworks, owner-only write checked
    against auth.uid(), public insert-only on reports (no public read)
- Created .env.example listing required Supabase and Cloudinary variable
  names (no real values).
- Confirmed .gitignore (from create-next-app) already covers .env*,
  node_modules, .next, etc.

DECISIONS MADE:
- Image pipeline: upload goes straight to Cloudinary for resize/compression
  and hosting/CDN; the resulting Cloudinary URL is stored in the database.
  Decided against also copying the file into Supabase Storage afterward --
  that would mean paying for storage and bandwidth twice for no benefit.
- Skills field is a simple free-text tag list (text[]), capped at 15 tags,
  no predefined taxonomy.
- Contact info uses a fixed set of optional fields: email, Instagram,
  Facebook, X, website -- not an open-ended list.
- Report reasons restricted to exactly: stolen_art, inappropriate, other.

STILL PENDING / NEXT TIME:
- Nimith needs to create the Supabase project and Cloudinary account, then
  hand over the env var values (see .env.example for the names needed).
- Migrations have been written but not yet run against a live Supabase
  project.
- No application code yet (auth flow, artist signup/slug generation,
  profile edit, artwork upload/reorder, homepage browse/search, report
  button).
- RLS policies still need to be tested with two real artist accounts once
  the project is live (log in as Artist A, confirm editing Artist B's data
  fails).
===========================================

===========================================
DATE: 7/2/2026
WORKED ON BY: Nimith (session run with Claude)
-------------------------------------------
DONE TODAY:
- Full audit pass of the project (logic, security, API, secrets, rate
  limiting). Main finding: no application code exists yet, so most checks
  apply to the database layer only. Secrets hygiene and RLS design passed
  review; a few database-level issues were found and then fixed after
  go-ahead:
- Fixed race condition in the 20-artwork cap trigger (BUG #001, see
  BUGLOG.txt).
- Added reserved-slug constraint on tbl_artists so nobody can register
  slugs like admin, api, login as their public page URL.
- Added a flood cap on tbl_reports (max 50 reports per artwork, enforced
  by trigger) so anonymous report inserts cannot bloat the database. Real
  per-visitor rate limiting still needs to be built into the report API
  route later.
- Pinned search_path = '' on all trigger functions and made the two
  counting triggers security definer (needed for their row locks, and
  standard Supabase hardening).
- Removed redundant tbl_artists_slug_idx (the unique constraint already
  creates an index) and added a sort_order range check (0-999).
- Tested everything against a local Postgres 16: all 4 migrations apply
  cleanly, plus 14 functional tests (cross-artist writes blocked by RLS,
  reserved slug rejected, artwork cap holds, reports invisible to anon,
  flood cap holds) and a two-session concurrency test proving the race
  fix works. All passed.

DECISIONS MADE:
- Report flood protection lives at the database level as a per-artwork
  cap (50); per-visitor rate limiting will be added in the API route
  when the report feature is built (proposed: Upstash Redis).
- Migrations were edited in place rather than adding new migration files,
  since they have never been run against a live Supabase project.

STILL PENDING / NEXT TIME:
- Everything from the 7/1 entry still stands: create Supabase + Cloudinary
  accounts, fill .env.local, run migrations, then start building the app
  (auth, profiles, upload, public pages, report route with rate limiting).
- Re-run the two-account RLS test against the live Supabase project once
  it exists (local test passed, but live config should be confirmed).
===========================================

===========================================
DATE: 7/2/2026 (second session, same day)
WORKED ON BY: Nimith (session run with Claude)
-------------------------------------------
DONE TODAY:
- Extended the schema per Nimith's go-ahead: image_public_id, width/height,
  medium, year_created, updated_at on tbl_artworks; photo_public_id on
  tbl_artists; status (new/reviewed) on tbl_reports. Re-ran the full local
  Postgres test suite: all migrations apply, all 15 tests pass.
- Built the entire MVP web app on Nimith's instruction to "build everything":
  - Auth: signup (with email-confirmation handling), login, logout via
    Supabase Auth; session refresh in src/proxy.ts.
  - Dashboard: create/edit profile (slug auto-suggested from name), profile
    photo upload, artwork upload/edit/reorder/delete with the 20 cap shown.
  - Public pages: homepage with artist browse + search by name/skill;
    artist page at /[slug] with gallery, contact links, report button.
  - API routes: artwork upload (server-side file validation by real file
    bytes, Cloudinary signed upload, orphan cleanup on failure), artwork
    delete (ownership-checked, cleans Cloudinary), profile photo, and
    anonymous reports (rate-limited 5 per 10 min per IP, in-memory).
  - Design: Fraunces + Inter (Google Fonts), lucide-react icons plus
    simple-icons for brand logos (lucide v1 dropped brand icons). No emoji.
- Verified: production build compiles clean, ESLint passes, and a running
  server smoke test passed (pages 200, dashboard redirects to login when
  logged out, APIs reject bad input/no auth, rate limiter returns 429).
  NOT yet tested against a real Supabase/Cloudinary account -- that needs
  the live keys.
- Added MAP.md (project file index) and removed CLOUDINARY_UPLOAD_PRESET
  from .env.example (server-signed uploads do not need a preset).

DECISIONS MADE:
- Uploads go through server API routes with signed Cloudinary uploads, so
  the Cloudinary secret stays server-side and files are validated by their
  real bytes, not the browser's claim.
- Rate limiting is in-memory per instance for now (documented limitation);
  swap for Upstash Redis later if abuse becomes real.
- Renamed middleware.ts to proxy.ts (Next 16 deprecated the middleware
  file convention).

STILL PENDING / NEXT TIME:
- Nimith: create Supabase + Cloudinary accounts, fill .env.local, run the
  4 migrations in the Supabase SQL editor, then test the real flows
  end-to-end (see chat for the exact step-by-step).
- GitHub push is still blocked (app has read-only access); grant write
  access so the branch can be pushed.
- After live setup: two-account RLS test, and consider Vercel deploy.
===========================================

===========================================
DATE: 7/2/2026 (third session, same day, parallel chat)
WORKED ON BY: Nimith (session run with Claude, separate conversation from
the "second session" entry above)
-------------------------------------------
DONE TODAY:
- Discovered that Nimith had two parallel Claude Code sessions running
  against what looked like the same project: this one (in the real,
  correctly-named artist-portfolio- folder, already containing the mature
  MVP described above) and a second conversation that had been working,
  unaware of this repo, inside a separate, differently-named folder
  ("Artist portfolio", with a space) doing a full duplicate rebuild from
  zero -- its own migrations, its own login/signup/dashboard/about pages,
  none of it connected to this repo or its GitHub history.
- With Nimith's go-ahead, deleted the duplicate folder entirely (it had no
  unique history -- confirmed via git log before removing anything).
- Ported the one genuinely useful thing from the duplicate work into this
  real project: a "Warm Gallery" visual design (cream/charcoal/terracotta
  color tokens, hover/entrance animations) that had been designed in
  response to Nimith flagging the flat, animation-less default look --
  the same flatness this real project's plain zinc/black-and-white styling
  had. Applied it as a pure styling pass across globals.css, layout.tsx,
  and every page/component in src/app -- no logic, data fetching, auth,
  or API route code was touched. Verified with git diff --stat that only
  the intended 11 files changed.
- Verified npm run build and npm run lint both still pass after the
  styling pass.
- Attempted a dev-server route check and got 500s on every route -- traced
  to the pre-existing, expected cause (src/proxy.ts throws because
  .env.local still does not exist), not anything from this session's
  changes. Confirmed via the dev server's own error log.

DECISIONS MADE:
- Chose to delete the duplicate folder rather than keep it "just in case,"
  since it was pure duplicate effort with no independent value once its
  one useful piece (the design) was ported over.
- Kept this project's existing --font-heading / --font-body CSS variable
  naming instead of renaming to match the duplicate's own convention, per
  "match what's already there."

STILL PENDING / NEXT TIME:
- Everything from the two entries above still stands: fill .env.local with
  real Supabase + Cloudinary keys, run the 4 migrations, then the 500s
  from proxy.ts should resolve and the real end-to-end testing can start.
- If another parallel session is still active on this same repo, the two
  should be reconciled so work does not get duplicated again.
===========================================

===========================================
DATE: 7/2/2026 (continued, same session as above)
WORKED ON BY: Nimith (session run with Claude)
-------------------------------------------
DONE TODAY:
- Live setup completed: Nimith created the Supabase project (Singapore
  region) and Cloudinary account, filled .env.local (all 6 values verified
  present and working -- Supabase REST and Cloudinary API both answered
  200 with the configured keys). Disabled "Confirm email" in Supabase Auth
  temporarily for easier testing -- MUST be re-enabled before launch.
- Found and fixed the first live bug: artwork upload failed with a missing
  image_height column. Root cause: the migrations Nimith originally ran in
  the SQL editor were the older duplicate-folder versions (pre-dating this
  repo's schema extensions), so the live tables were missing 6+ columns,
  the race-safe artwork trigger, and the reports status column. Fix: one
  combined reset script (drop the 3 tables + re-run this repo's 5
  migrations); Nimith ran it and the schema now matches the code (verified
  column-by-column via REST). The throwaway reset file was deleted after.
- Added Telegram to artist contact links end-to-end: new migration
  20260702000001_add_telegram_url.sql (run live), Artist type, dashboard
  form field, public page icon (simple-icons siTelegram).
- Made the social link fields honestly optional: switched them from
  type="url" to type="text" with placeholder examples, because the
  browser's native URL validation was blocking saves on fields that are
  not required at all.
- Built dark mode: class-based (.dark on <html>), token overrides in
  globals.css (warm dark brown palette, brighter terracotta accent),
  stateless Sun/Moon toggle in the header (CSS dark: variant picks the
  icon, so no hydration mismatch), localStorage persistence, follows the
  OS setting on first visit, pre-paint inline script prevents the white
  flash. Verified in the served HTML/CSS.

DECISIONS MADE:
- Database fix was a full drop-and-recreate rather than per-column ALTER
  patches, since only one test profile existed and a reset guarantees the
  live schema exactly matches the migration files.
- Dark palette stays warm (brown-black, not pure black) to keep the
  gallery character; accent brightened for contrast on dark surfaces.

STILL PENDING / NEXT TIME:
- Nimith to re-create his profile (wiped by the reset) and confirm artwork
  upload to Cloudinary works end-to-end -- both tables were still empty at
  the time of this entry.
- Re-enable "Confirm email" in Supabase Auth before real users sign up.
- Google sign-in, homepage pagination, two-account RLS test, Vercel
  deploy.
===========================================
